---
import { Socials } from "@/components/ui/socials";
---

<div class="max-w-md mx-auto px-4 text-lg text-foreground">
    <section
        class="flex flex-col items-center justify-center text-center pt-40"
    >
        <h1 class="text-4xl font-bold text-foreground">
            hi, I'm Lorenzo 👋
        </h1>
        <p class="text-lg text-foreground mt-3">
            I'm <span id="typed-text"></span>
        </p>
    </section>

    <section class="flex flex-col items-center justify-center text-center mt-3">
        <p class="text-lg text-foreground mb-8">
            Creating games that i would play.
        </p>
        
        <div class="relative group">
            <div
                class="absolute -inset-0.5 bg-gradient-to-r from-primary to-secondary rounded-full blur opacity-75 group-hover:opacity-100 transition duration-300"
            ></div>
            <div
                class="relative size-36 rounded-full overflow-hidden border-2 border-background"
            >
                <img 
                    src="/images/headshot/lore-headshot.png" 
                    alt="Lore's Profile" 
                    class="w-full h-full object-cover scale-110 object-[center_bottom]"
                    loading="lazy"
                    decoding="async"
                />
            </div>
        </div>
    </section>
    <section class="flex flex-col items-center justify-center text-center mt-3">
        <Socials />
    </section>
</div>

<style>
    .typewriter-cursor {
        display: inline-block;
        margin-left: 2px;
        animation: blink 0.7s infinite;
    }

    @keyframes blink {
        0% {
            opacity: 1;
        }
        50% {
            opacity: 0;
        }
        100% {
            opacity: 1;
        }
    }
</style>

<script>
    import EmojiTypewriter from "../lib/emoji-typewriter.js";

    // Initialize the typewriter when the DOM is loaded
    document.addEventListener("DOMContentLoaded", () => {
        const element = document.getElementById("typed-text");

        if (element) {
            const typewriter = new EmojiTypewriter({
                words: [
                    "a mobile game developer 🎮", // Enfatizza lo sviluppo di giochi
                    "a curious creator ⚡",     // Riprende dalla lista originale, si adatta al creatore di giochi/prodotti
                    "a padel player 🎾",         // Richiesta specifica
                    "a cat lover 🐱"         
                ],
                typeSpeed: 50,
                deleteSpeed: 30,
                delaySpeed: 2500,
                loop: true,
                cursor: true,
                cursorChar: "|",
                cursorBlinkSpeed: 700,
            });

            typewriter.init(element);
        }
    });
</script>
